The following commands were used during the handling of the nginx container in docker!

## docker ps / docker ps -a
`docker ps` is used alongside `docker ps -a` to check for active and inactive containers.
## docker stop
`docker stop` is used to stop any active containers.
## docker rm
`docker rm` is used to remove any inactive containers though can force remove any active ones with flag `-f`.